[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "gunicorn app:app"

[build]
nixPkgs = ["python311"]

[start]
cmd = "gunicorn app:app"